---
layout: layout.liquid
title: Zoom Video Conferencing
description: Here you will find everything you need to access Zoom.
type: landing
---
<div class="links">
    {% for link in links.Zoom.Body %}
        <div class="container">
            <div class="link">
                <a class="button1" href="{{link.Url}}">{{ link.Title }}</a>
            </div>
        </div>
    {% endfor %}
    {% for link in links.Zoom.Footer %}
        <div class="container">
            <div class="link">
                <a class="button1" href="{{link.Url}}">{{ link.Title }}</a>
            </div>
        </div>
    {% endfor %}
</div>